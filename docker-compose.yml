version: "3"
services:
  backend:
    image: node:alpine
    volumes:
      - ./backend/:/app
    working_dir: /app
    depends_on:
      - mongo
    environment:
      NODE_ENV: development
      MONGO_HOST: mongo
      PORT: 8888
    ports:
      - 8888:8888
    command: npm run dev
  mongo:
    image: mongo
    expose:
      - 27017
    volumes:
      - data:/data/db
volumes:
  data:
